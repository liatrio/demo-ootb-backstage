# Contributing to the Demo Backstage Out Of the Box for Autogov AutoGov Plugins

## Local Testing

1. Install dependencies

   ```zsh
   yarn install
   ```

1. Run Dev Environment

   ```zsh
   yarn dev
   ```
